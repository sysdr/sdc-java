server:
  port: 8080

spring:
  application:
    name: log-gateway

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true
