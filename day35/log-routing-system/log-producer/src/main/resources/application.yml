server:
  port: 8080

spring:
  application:
    name: log-producer

routing:
  service:
    url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true
