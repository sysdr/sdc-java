spring:
  application:
    name: dashboard-service

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.example.dashboard: INFO

